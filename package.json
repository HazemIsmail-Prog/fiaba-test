{
  "name": "fiaba",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix website",
    "build": "npm run build --prefix website && npm run build --prefix frontend && npm run build --prefix backend && node scripts/copy-static.mjs",
    "start": "node backend/dist/index.js",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend && npm run typecheck --prefix website"
  }
}
